<!DOCTYPE html>
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU", crossorigin="anonymous")
        title #{pageTitle} | #{siteName}
    body
        header.header
            .header__wrapper
                .header__column
                    a(href="javascript:history.back();")
                        i.fas.fa-arrow-left
                .header__column
                    p 메뉴상세
        main
            .menu-detail__containerS
                .menu__image
                    img.menu-img(src=items.itemUrl)
                .menu__description
                    h4.menu-des=items.description
                .menu__info
                    h4.menu-name=items.itemName
                    h4.menu-price=items.itemPrice
            .shopping-basket__btn
                button.shopping-basket__btn
                    a(href=routes.containItem(items.id))
                        span 장바구니 담기
        footer.footer
            .footer__icon
                i.fas.fa-concierge-bell
            span.footer__text #{siteName} #{new Date().getFullYear()} &copy;